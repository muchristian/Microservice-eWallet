
FROM node:16.16-slim

ENV NODE_ENV=
ENV PORT=
ENV POSTGRES_PORT=
ENV POSTGRES_HOST=
ENV POSTGRES_USER=
ENV POSTGRES_PASSWORD=
ENV POSTGRES_DB=

WORKDIR /app/apis

COPY . .

RUN yarn install

EXPOSE 3000

RUN yarn build

CMD [ "yarn", "start:dev" ]